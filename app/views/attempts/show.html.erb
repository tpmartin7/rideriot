<div class="container mt-4 mb-4 mb-4 mt-4">
    <div id="map"
      style="width: 100%; height: 600px;"
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY']%>">
    </div>
  <div class="text-center">
    <button type="button" class="btn btn-success mt-3">Start Route</button>
  </div>
</div>
