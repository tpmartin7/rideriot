const routeTime = () => {
  const header = document.getElementById("route-time");
  const button = document.getElementById("map-btn");
  // button.addEventListener("click", () => {
  //   header.insertAdjacentHTML("beforeend", "some seconds");
  // });
}

export { routeTime }
